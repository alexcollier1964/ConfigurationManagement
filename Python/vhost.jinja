{# Nginx virtual host server block template for Configuration Management. 
Expects a set of config items and will generate a single output file for the set specified #}# Nginx virtual host server block Configuration File, automatically generated by {{ generatedby }}upstream {{ templatevalues.custapp }} {
    server  {{ templatevalues.upstream }}       
}

server {
    listen 443;
    server_name  {{ templatevalues.external }};
    ssl_certificate     {{ templatevalues.external }}.crt;
    ssl_certificate_key {{ templatevalues.external }}.key;

    access_log logs/{{ templatevalues.cust }}/{{ templatevalues.apptag }}/{{ templatevalues.external }}.access.log main;

    location / {
        proxy_pass http://{{ templatevalues.custapp }};
    }
}
